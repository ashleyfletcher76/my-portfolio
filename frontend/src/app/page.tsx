export default function Home() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center gap-6 bg-white text-gray-900 dark:bg-gray-900 dark:text-gray-100">
            <h1 className="text-4xl font-bold">Tailwind + Dark Mode Test</h1>
            <p>Toggle dark mode manually for now (in dev tools).</p>
        </main>
    );
}